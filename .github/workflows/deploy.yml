name: Deploy

on:
  workflow_run:
    workflows: ["Build"]
    types:
      - completed

jobs:
  review-app:
    runs-on: ubuntu-latest
    if: >
      github.event.workflow_run.event == 'pull_request' &&
      github.event.workflow_run.conclusion == 'success'
    permissions:
      actions: read
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: build-${{ github.event.workflow_run.head_commit.id }}
          run-id: ${{ github.event.workflow_run.id }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          path: .

      - run: |
          ls -a
      - name: Deploy on CC
        uses: CleverCloud/clever-cloud-review-app@optional-comment
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CLEVER_SECRET: ${{ secrets.CLEVER_SECRET }}
          CLEVER_TOKEN: ${{ secrets.CLEVER_TOKEN }}
          ORGA_ID: ${{ secrets.ORGA_ID }}
          GH_CC_WEBROOT: '/public'
        with:
          type: static-apache
          domain: ${{ github.event.workflow_run.pull_requests[0].number }}.cleverapps.io
          comment: false
# Deploy the artifact to Clever Cloud
# Use CC_WEBROOT or git init in the artifact directory?
# Tests: ls -a in the runner